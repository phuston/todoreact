var express = require('express');
var router = express.Router();

/* GET all todos. */
router.get('/todos', function(req, res, next) {
	// Get all TODOs here
});

router.post('/todos', function(req, res, next) {
	// Add a new TODO object here
})

module.exports = router;
